info:
  name: GetMessages
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/messages
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
